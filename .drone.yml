clone:
  git:
    image: registry.fpfis.eu/drone-plugins/git:next
    when:
      event: [ push, pull_request, tag ]
  git:
    image: registry.fpfis.eu/drone-plugins/git
    when:
      event: [ deployment ]

workspace:
  base: /test
  path: oe_translation

services:
  web:
    image: registry.fpfis.eu/fpfis/httpd-php:${PHP_VERSION}-ci
    environment: &web-environment
      - DOCUMENT_ROOT=/test/oe_translation
      - COMPOSERVER=--2
      - EPOETRY_SERVICE_URL=http://web:8080/build/epoetry-mock/server
      - EPOETRY_APPLICATION_NAME=digit
      - EPOETRY_AUTH_CERT_SERVICE_URL=""
      - EPOETRY_AUTH_CERT_PATH=""
      - EPOETRY_AUTH_CERT_PASSWORD=""
      - EPOETRY_AUTH_EU_LOGIN_BASEPATH=""
      - CDT_TOKEN_API_ENDPOINT=""
      - CDT_MAIN_API_ENDPOINT=""
      - CDT_REFERENCE_DATA_API_ENDPOINT=""
      - CDT_VALIDATE_API_ENDPOINT=""
      - CDT_REQUESTS_API_ENDPOINT=""
      - CDT_IDENTIFIER_API_ENDPOINT=""
      - CDT_STATUS_API_ENDPOINT=""
      - CDT_FILE_API_ENDPOINT=""
      - CDT_CLIENT=""
      - CDT_USERNAME=""
      - CDT_PASSWORD=""
      - CDT_API_KEY=""
  mysql:
    image: git.fpfis.tech.ec.europa.eu/fpfis/dependency_proxy/containers/percona:5.7
    command: --innodb-log-file-size=1G --max_allowed_packet=1G --innodb-buffer-pool-size=512M --wait_timeout=3000 --net_write_timeout=3000 --log_error_verbosity=3
    environment:
      - MYSQL_ALLOW_EMPTY_PASSWORD=yes
  selenium:
    image: git.fpfis.tech.ec.europa.eu/fpfis/dependency_proxy/containers/selenium/standalone-chrome:4.1.3-20220405
    shm_size: 2g
    environment:
      - DISPLAY=:99
      - SCREEN_WIDTH=1280
      - SCREEN_HEIGHT=800
      - NODE_MAX_INSTANCES=5
      - NODE_MAX_SESSION=5

pipeline:
  composer-change-patch:
    group: warmup
    image: registry.fpfis.eu/fpfis/httpd-php:${PHP_VERSION}-ci
    volumes:
      - /cache:/cache
    commands:
      # Change the patch for 10.1.x.
      - >
        composer config --merge --json "extra.patches.drupal/core" '{"https://www.drupal.org/project/drupal/issues/2230909": "https://www.drupal.org/files/issues/2022-07-26/2230909-269.patch"}'
      - >
        composer config --merge --json "extra.patches-ignore.openeuropa/oe_content" '{"drupal/core": {"Drupal 10.2.x only - see README.md for 10.1.x - https://www.drupal.org/project/drupal/issues/2230909": "https://www.drupal.org/files/issues/2023-12-21/2230909-309.patch"}}'
    when:
      matrix:
        CORE_VERSION: "10.1.0"

  composer-install:
    group: prepare
    image: registry.fpfis.eu/fpfis/httpd-php:${PHP_VERSION}-ci
    volumes:
      - /cache:/cache
    commands:
      - apt update -y
      - apt install php${PHP_VERSION}-bcmath -y
      - composer self-update --2
      - composer require drupal/core:~${CORE_VERSION} drupal/core-composer-scaffold:~${CORE_VERSION} drupal/core-dev:~${CORE_VERSION} --update-with-all-dependencies --ansi --no-progress --no-interaction

  ensure-phpunit-batching:
    group: test
    image: registry.fpfis.eu/fpfis/httpd-php:${PHP_VERSION}-ci
    commands:
      - ./vendor/bin/phpunit --group default
    environment: *web-environment

  site-install:
    image: registry.fpfis.eu/fpfis/httpd-php:${PHP_VERSION}-ci
    commands:
      - while ! mysqladmin ping -h mysql --silent; do sleep 1; done
      - ./vendor/bin/run drupal:site-install

  grumphp:
    group: test
    image: registry.fpfis.eu/fpfis/httpd-php:${PHP_VERSION}-ci
    commands:
      - ./vendor/bin/grumphp run

  phpunit:
    group: test
    image: registry.fpfis.eu/fpfis/httpd-php:${PHP_VERSION}-ci
    environment: *web-environment
    commands:
      - ./vendor/bin/phpunit --group $BATCH

  debug:
    image: registry.fpfis.eu/fpfis/httpd-php:${PHP_VERSION}-ci
    commands:
      - ./vendor/bin/drush ws --count 500
    when:
      status: failure

matrix:
  include:
    - { PHP_VERSION: 8.1, CORE_VERSION: 10.1.0, TYPE: phpunit, BATCH: batch1 }
    - { PHP_VERSION: 8.1, CORE_VERSION: 10.1.0, TYPE: phpunit, BATCH: batch2 }
    - { PHP_VERSION: 8.1, CORE_VERSION: 10.2.0, TYPE: phpunit, BATCH: batch1 }
    - { PHP_VERSION: 8.1, CORE_VERSION: 10.2.0, TYPE: phpunit, BATCH: batch2 }
    - { PHP_VERSION: 8.2, CORE_VERSION: 10.1.0, TYPE: phpunit, BATCH: batch1 }
    - { PHP_VERSION: 8.2, CORE_VERSION: 10.1.0, TYPE: phpunit, BATCH: batch2 }
    - { PHP_VERSION: 8.2, CORE_VERSION: 10.2.0, TYPE: phpunit, BATCH: batch1 }
    - { PHP_VERSION: 8.2, CORE_VERSION: 10.2.0, TYPE: phpunit, BATCH: batch2 }
